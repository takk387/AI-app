# Git Workflow

## Branching Policy

**Always push changes directly to main. Do not create feature branches unless explicitly asked.**

When making changes:

1. Work on `main` directly
2. Commit and push to `origin/main`
3. Only create a branch if the user explicitly requests it (e.g., "create a branch for this", "open a PR")
