# =============================================================================
# AI App Builder - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control

# =============================================================================
# Required - Core Services
# =============================================================================

# Anthropic API Key (for Claude AI)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# =============================================================================
# Optional - Debug Flags (Development Only)
# =============================================================================
# These flags ONLY work when NODE_ENV=development
# They are automatically disabled in production builds

# Mock AI Mode - Skip real Claude API calls (instant responses, no API cost)
# Use for testing UI without waiting for AI or using credits
# WARNING: When enabled, AI responses are fake/placeholder
# NEXT_PUBLIC_MOCK_AI=true

# Show Debug Panel - Floating overlay showing state, API calls, tokens
# NEXT_PUBLIC_DEBUG_PANEL=true

# Expose State to Console - Access state via window.__APP_STATE__
# NEXT_PUBLIC_DEBUG_STATE=true

# Log API Calls - Print all API requests/responses to console
# NEXT_PUBLIC_DEBUG_API=true

# Log Token Usage - Print token counts for each AI request
# NEXT_PUBLIC_DEBUG_TOKENS=true

# Log Phase Execution - Print phase start/complete/error events
# NEXT_PUBLIC_DEBUG_PHASES=true

# =============================================================================
# Quick Start Commands
# =============================================================================
# Normal development:     npm run dev
# With mock AI:           npm run dev:mock
# With all debug flags:   npm run dev:debug
